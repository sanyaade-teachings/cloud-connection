<?xml version="1.0" encoding="UTF-8"?>
    <!--
        Import per project custom build rules if present at the root of the project.
        This is the place to put custom intermediary targets such as:
            -pre-build
            -pre-compile
            -post-compile (This is typically used for code obfuscation.
                           Compiled code location: ${out.classes.absolute.dir}
                           If this is not done in place, override ${out.dex.input.absolute.dir})
            -post-package
            -post-build
            -pre-clean
    -->
<project>    
	<target name="-pre-build">
	   <echo>**** -pre-build *****</echo>
     
     <echo>arch= ${arch}</echo>
     <echo>mainlib= ${mainlib}</echo>
     
    <!-- copy the native binaries that were built in visual studio into the /libs folder -->       
    
    <!-- TODO : copy the lib to correct folder (arm or x86) -->
    <copy todir="./libs/armeabi-v7a">
      <fileset dir="${mainlib}">
      </fileset>
    </copy>      
    
    <!-- TODO : copy any other .so libs required to run the game -->
    
    <!-- TODO : emulate the asset copy system that from deployAndroid.py "def copyAssets(options, fatOptions=None):" -->
    
    <!-- copy the game assets /Assets folder -->       
    <copy todir="./assets/Assets">
      <fileset dir="../../../../Assets/">
      </fileset>
    </copy>      
    
    <!-- copy the vision base assets /Data folder -->       
    <copy todir="./assets/Data">
      <fileset dir="../../../../Data/">
      </fileset>
    </copy>      
    
	</target>
	
	<target name="-pre-compile">
	    <echo>**** -pre-compile *****</echo>
	</target>
	
	<target name="-post-compile">
	    <echo>**** -post-compile *****</echo>
	</target>    
	
	<target name="-post-package">
	    <echo>**** post-package *****</echo>
	</target> 
	
	<target name="-post-build">
	    <echo>**** -post-build *****</echo>
	</target> 
	
	<target name="-pre-clean">
	    <echo>**** -pre-clean *****</echo>
	</target>
</project>